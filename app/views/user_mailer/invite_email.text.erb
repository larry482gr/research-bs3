<%= (t 'email_deliver.message.invitation.header') %>
===============================================

<%= (t 'email_deliver.message.invitation.par_one').gsub('%username%', @current_user.username).gsub('%project_title%', @project.title) %>

<%= (t 'email_deliver.message.invitation.par_two') %>
