<%= (t 'email.message.invitation.header') %>
===============================================

<%= (t 'email.message.invitation.par_one').gsub('%username%', @current_user.username).gsub('%project_title%', @project.title) %>

<%= (t 'email.message.invitation.par_two') %>