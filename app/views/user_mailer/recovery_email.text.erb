<%= (t 'email.message.invitation.header').html_safe %>
===============================================

<%= (t 'email.message.invitation.par_one').gsub('%username%', @current_user.username).gsub('%project_title%', @project.title).html_safe %>

<%= (t 'email.message.invitation.par_two').html_safe %>